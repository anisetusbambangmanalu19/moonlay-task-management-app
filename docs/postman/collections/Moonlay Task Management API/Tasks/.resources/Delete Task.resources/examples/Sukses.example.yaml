$kind: http-example
request:
  url: "{{base_url}}/tasks/{{task_id}}"
  method: DELETE
  headers:
    Authorization: Bearer {{token}}
response:
  statusCode: 200
  statusText: OK
  body:
    type: text
    content: |-
      {
        "message": "Task berhasil dihapus"
      }
order: 1000
